"""Echo v2 service layer.

Application services that orchestrate domain logic, provider ports, and
persistence. Services are the glue between the provider boundary
(``echo_v2.ports``), the domain layer (``echo_v2.domain``), the runtime
(``echo_v2.runtime``), and persistence (``echo_v2.persistence``).
"""
