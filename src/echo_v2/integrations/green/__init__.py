"""Green API integration for Echo v2.

Implements the provider-neutral ports in :mod:`echo_v2.ports.whatsapp` against
the Green API partner/instance endpoints. Green-specific identifiers
(``idInstance``, ``apiTokenInstance``, ``stateInstance``) live only inside
this package.
"""
